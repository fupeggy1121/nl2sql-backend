# NL2SQL 前端配置文件 - 复制为 .env.local 使用

# ==========================================
# API 配置
# ==========================================

# 方案1：本地开发（仅限本地网络）
# VITE_API_URL=http://localhost:8000/api/query

# 方案2：内网 IP（仅限本地网络）
# VITE_API_URL=http://192.168.2.13:8000/api/query

# 方案3：Cloudflare Tunnel（临时 URL，每次重启变化）
# VITE_API_URL=https://colored-hypothesis-animated-toddler.trycloudflare.com/api/query

# 方案4：Cloudflare Tunnel（固定 URL - 推荐）
# 注：需要先在 Cloudflare 配置固定域名
# VITE_API_URL=https://nl2sql-backend.fupeggy.workers.dev/api/query

# ==========================================
# 调试选项
# ==========================================

# 启用 API 请求日志
VITE_DEBUG_API=true

# ==========================================
# React/Vite 相关
# ==========================================

# 开发模式
VITE_ENV=development

# 生产模式（部署时使用）
# VITE_ENV=production
